package gts.spring.registration.repository;

import gts.spring.registration.entity.ScheduledClass;
import org.springframework.stereotype.Repository;

@Repository
public interface ScheduledClassRepository extends BaseRepository<ScheduledClass> {
}
